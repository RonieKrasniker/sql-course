# SQL & Databases — Course Exercises

Database coursework at Bar-Ilan University. Source code only.

- **EX1** — Python-driven SQL queries over a Formula-1 dataset (joins, filtering, aggregation).
- **EX2** — a second query assignment with a small test harness.
- **EX3** — schema design: an ER diagram (`erd.mmd`) plus query and generation scripts.

_Only my own source code — no lecture slides, assignment PDFs, or submission archives._
